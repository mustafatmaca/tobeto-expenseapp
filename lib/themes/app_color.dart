import 'package:flutter/material.dart';

class AppColor {
  static ColorScheme colorScheme =
      ColorScheme.fromSeed(seedColor: Colors.amber);
  static Color primaryColor = Colors.amber;
  static Color secondaryColor = Colors.deepOrange;
  static Color mainWhite = Colors.grey[100]!;
  static Color mainBlack = Colors.grey[900]!;
}
